name: first workflow
on: push  # [ push, pull ]
jobs: 
    first_job: 
        runs-on: ubuntu-latest
        steps:
            - name: welcome message
              run: echo "My first github actions job"
            - name: list files
              run: list
            - name: read files
              run: cat README.md
              
