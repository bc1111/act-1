name: first workflow
on: push  # [ push, pull ]
jobs: 
    first_job: 
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout
            - name: welcome message
              run: echo "My first github actions job"
            - name: list files
              run: ls
            - name: read files
              run: cat README.md
              
